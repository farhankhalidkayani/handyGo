name: handygo_shared
description: Shared models, booking status enum, and Appwrite client config for the HandyGo customer/worker/admin apps.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  appwrite: ^25.3.0
  web: ^1.0.0
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1

dev_dependencies:
  test: ^1.24.0
  flutter_test:
    sdk: flutter
